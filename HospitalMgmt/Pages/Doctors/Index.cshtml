@page
@model HospitalMgmt.Pages.Doctors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Doctor_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Specialists)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Availability)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Patient_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Doctor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Specialists)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Availability)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Patient_ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Doctor_Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Doctor_Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Doctor_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
