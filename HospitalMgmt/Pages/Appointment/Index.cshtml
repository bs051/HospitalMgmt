@page
@model HospitalMgmt.Pages.Appointment.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Appointments[0].Patient_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Appointments[0].Doctor_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Appointments[0].Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Appointments) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Patient_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Appointment_Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Appointment_Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Appointment_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
